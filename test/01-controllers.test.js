import { BaseController } from '../src';

describe('01_Controllers', () => {
  it('New BaseController', () => {
    const controller = new BaseController();
    console.log(controller);
    // mockController = new MockController();
  });
});
